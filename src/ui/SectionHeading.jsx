function SectionHeading({ children }) {
  return (
    <h2 className="xs:text-xl mb-10 text-lg font-semibold uppercase tracking-wider md:mb-14 md:text-2xl">
      {children}
    </h2>
  );
}

export default SectionHeading;
